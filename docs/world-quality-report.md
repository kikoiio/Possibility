# s01 实施质量报告

最后核对：2026-09-24。此报告记录本地实现证据和未满足的门槛，不代表 S01 全局验收已完成。

## 本轮已验证

- `npm --workspace api test`：224 项通过；`npm --workspace web test`：9 项通过。自动测试共 233 项。
- `npm --workspace api run build`：通过。
- `npm --workspace web run build`：通过。
- `git diff --check`：通过。
- Cloudflare D1 staging：迁移 0000–0018 已全部应用；远端 Wrangler 4.126 的 `migrations apply` 将含有多个触发器的 migration 合并为单个 SQL，Cloudflare 拒绝 0008 并回滚该迁移。`npm run apply:s01:remote-test-migrations` 按仓库 `statement-breakpoint` 边界逐条执行，并安全限制目标配置名必须包含 test/staging/acceptance；迁移账本 19/19 且无待应用项。两个独立 `wrangler dev --remote` Worker 共用此 D1，最后一个 active Fork 名额的并发请求得到 200/409；跨 Worker 同请求重放 200、改载荷冲突 409、超容量 409。两 Worker `/api/engine/tick` 在 D1 由外部 owner 持锁时均返回 409 且 revision/clock fact 不变；两独立 D1 命令的原子租约 UPSERT 只有一个 owner；直接越级 revision 写入被真实 D1 trigger 拒绝。Worker、lease 和 Fork 临时记录在脚本结束时清理；脚本通过 `S01_REMOTE_D1_CONFIG` 与 `S01_REMOTE_D1_ID` 显式指定目标，不将账户凭据写入仓库。
- 有结构化 revision 的宇宙若缺失其指向的固定模型版本，此前普通读取和行动校验会得到 `null`，继而退回可编辑资产资料。现在模型读取明确报冲突；主人/公开快照返回 409，世界行动被拒且不写事实。`model_json` 为 `null`、非法 JSON 或缺少必要顶层字段时也拒绝读取，审计报告 `world_model_version_invalid`；回归覆盖 `null` 内容。没有 revision 的 legacy 世界继续走兼容读取。API 224 项及构建通过。仍不能把这视为修复损坏数据本身，需保留审计定位与人工修复边界。
- 旧聊天与 Fork 预览先前直接使用可编辑的 `persons.modelJson`，即使引擎已固定宇宙模型，也可能在共享人物卡改动后呈现另一套性格设定。`buildAgentContext` 现按时间线读取固定的人物模型、名字和世界描述；只有没有固定记录的 legacy 世界继续读取当前资产行。定向回归先在世界 A/B 各自固定不同人物设定，再修改共享人物卡，确认两世界的旧聊天上下文仍分别读取原固定内容。API 203 项与构建通过；世界中后加入、未纳入原模型快照的人物仍需另行定义设定版本更新语义。
- T03 的精确时间线定位补上空字符串边界：世界/公开快照、对话详情、章节列表/生成、在场看板/历史/发送、身份收件箱与归来回顾仅在参数真正省略时使用主线默认。显式空 ID 不再按 falsy 值回落；章节列表的异世界/不存在 ID 返回 404，而不是伪装成空列表。定向路由回归与 API 全套 203 项、API 构建通过；兼容的省略 ID 默认路径未改变。
- 旧聊天发送原先先写用户消息、后检查世界状态/预算；暂停、归档或触顶会拒绝模型调用，却已改变聊天历史。现在发送前先核对所属世界、时间线和预算；已存在对话的 get-or-create 仍可在暂停世界返回 ID 供历史读取，新建对话则受同一闸门约束。定向回归确认暂停/归档拒绝后消息行不变。共享人物的旧聊天上下文也改为按显式时间线选世界，省略时间线才沿用最早加入世界的兼容默认；回归确认第二世界的状态和聊天入口可用。API 全套 202 项、API 构建通过；仍未覆盖请求发出后世界状态并发变化的所有旧聊天竞态。
- 旧聊天模型流中途失败时，服务端此前仍会把已收到的半截文本保存为完整人物回复，前端也会把本地临时气泡留在历史中。现在只有 `done` 无错误才持久化回复；发送结束或断流时前端重新读取服务端聊天记录，无法核对时去掉临时气泡并明确提醒不要立即重发。定向路由测试让模型在输出半句后断流，确认用户消息存在但没有伪完整人物消息。尚无旧聊天的持久 request ID / 跨设备精确重试机制，不能据此宣称 AC08 全通过。
- `WorldView` 的宇宙切换现重置本地事件、发言、居民增量状态、时钟、人物详情和已展开对话；旧 SSE 订阅及旧人物/对话请求的异步结果不会覆盖新线，世界状态面板按宇宙重新装载。快照追赶时只接纳同线且不倒退版本的结果。P4 的快速切线竞态已另在本地浏览器用延迟旧快照复核；AC10 新增 Root→Child→Grandchild 对话、事件、记忆、承诺和知识的同一 checkpoint cutoff 矩阵，仍缺共享人物旧数据矩阵。
- Fork 子宇宙的首页事件流和人物详情此前没有复用 checkpoint 可见性：前者只查当前 timeline 的 events，后者只查本线 memory bucket。现已修正 `worlds/queries.ts`：结构化 Fork 从不可变 checkpoint 读共同历史、合并本线事件；legacy Fork 仅按可信祖先截止点读历史；人物抽屉复用 `visibleMemories`，继承记忆可见且分叉后祖先新增内容不串入。`compare.test.ts` 的嵌套 Fork 产品查询覆盖孙线人物抽屉和首页事件流；新增组合矩阵则逐项确认 Root→Child 与 Child→Grandchild 的对话/事件/记忆/承诺/知识 cutoff，并审计三条时间线均无不变量错误。共享人物旧数据矩阵仍待完成。
- 对话事件也曾通过 `dialogueId` 直接读取源线现行 turns，导致 Fork 后源线新增回合可能改写子线看到的“共同过去”。现已把可见 dialogue/turn 行冻结进 Fork checkpoint；首页预览和展开详情按当前 timeline 读快照，子线后续 Fork 继续沿用祖先快照，旧快照没有 transcript 时不回退读取可变祖先。嵌套 Fork 回归确认子/孙只看到分叉前台词、主线仍能看到后来新增的台词。API 200 项测试、API 构建、Web 构建和 `git diff --check` 通过。
- 旧世界第一次建立结构化 revision 时，故事 `events` 没有 `createdAt`，仅用模拟时间无法区分“基线前的旧事件”和“同一模拟时刻后来直写的孤儿事件”。新建的根线模型基线现在冻结已有事件 ID；首次为旧世界创建模型基线时也只记录该刻已有的根线事件 ID，审计不会把它们误报成孤儿，而同时间点基线后新增、无命令来源的事件仍可检测。既有不可变模型 JSON 为兼容保持原样；没有 `initialEvents` 的旧模型不被推断为完整事件基线。`invariants.test.ts` 聚焦回归覆盖“基线前事件 + 同模拟时刻基线后孤儿事件”。
- `wrangler d1 migrations apply DB --local --persist-to /tmp/possibility-s01-revision-guard-20260923` 未能启动：沙箱拒绝 Wrangler 写入只读的 `/home/neo/.config/.wrangler` 日志目录并禁止绑定 `127.0.0.1`。命令未访问项目默认/远端 D1；新迁移由 `createTestDb` 的全量迁移 fixture 加载并经过直接 SQLite trigger 回归验证。
- 本地浏览器人工检查：使用合成验收账号完成不调用生成服务的人物卡与世界创建、主线构造、带明确 what-if/改变变量的 Fork、Compare、返回主线及居民口信；P4 另覆盖真实 tick、普通 AI 对话完成/断流后恢复、迟到聊天回复切线隔离及延迟旧快照隔离。所有浏览器旅程使用固定模型替身，今日调用为 0；凭据未保存到仓库。
- AC13 登录态 UI 走查发现并修复模式切换后旧交流面板仍可操作的问题；构造/在场/观察边界由浏览器及 `PersonDrawer.test.tsx` 覆盖。新增真实运行态记忆实测：隔离 D1 插入 timeline memory，构造模式校正后刷新仍见新内容、revision 从 0 到 1；观察模式只读且无改/删操作；构造模式遗忘后 D1 行删除、revision 到 2。摘要不可单独编辑/删除由组件与 API 回归覆盖。
- Fork 创建现在要求明确 `whatIf` 和 `changedVariable`：API 路由从服务端读取源线当前模拟时间，拒绝缺失/无效条件；同 requestId 但条件不同返回冲突。浏览器创建了带条件的分支，并在比较页看到该条件。已有旧 Fork 仍可读取。
- Compare 在默认选项中将当前子线与直接父线对照，减少多级谱系中默认挑中无关分支的情况；浏览器往返检查确认返回后仍处于主宇宙。
- AC06 在场传话的提交器此前只检查接收者占用，未检查访客自己是否正处于另一场交谈；现在拒绝忙碌访客传话，固定路由回归确认没有命令/事实产生且宇宙版本不变。此规则在服务端提交边界生效，不依赖前端状态。
- Fork 投影审计此前只从事实回放承诺变化，不校验子线初始复制的开放承诺；现在用不可变源快照匹配子线新 ID，再重放子线事实。故障注入篡改副本细节会定位到 fork baseline/version 0，无来源的额外承诺也会被报告；正常副本通过审计。
- `0008_cool_spiral.sql`、`0009_immutable_world_history.sql`、`0010_scene_request_payload_hash.sql`、`0011_universe_revision_sim_time.sql` 及此前迁移在隔离 D1 首次执行成功；`0009` 创建 6 条不可变触发器；`0010` 增加场景请求内容指纹。新增 `0012_dialogue_recovery_guard.sql` 已应用到隔离副本 `/tmp/possibility-d1-s01-final-20260923` 并核对迁移账本及触发器存在。当前沙箱阻止 Wrangler 绑定 loopback 端口，因此该增量使用 SQLite 对同一 Wrangler 本地 D1 文件事务式验证；未访问远端数据库或项目默认数据库。
- 新增 `0013_immutable_fork_provenance.sql`，并已应用到上述**隔离** Wrangler D1 SQLite 文件（迁移账本 id 14）。在隔离库内临时创建探针 Fork，确认改写 `fork_snapshot_json` 被 `fork_provenance_immutable_update` 拒绝，而正常归档状态更新仍允许；探针事务已回滚。固定 API 数据库回归也验证 snapshot、scenario、parent、ancestor IDs 均不可改，且普通 lifecycle 状态可更新。未运行项目默认 D1 迁移、未访问远端数据库。
- 新增 `0014_fork_schedule_checkpoint_guard.sql`，已在相同隔离副本记录为迁移账本 id 15。守卫验证快照中的当前/未来日程集合与源线在插入 Fork 时仍完全一致；API 回归模拟“快照捕获后、子线插入前新增日程”，旧快照被拒绝且不创建子线。这样覆盖了日程直写不递增宇宙版本所留下的竞争窗口。
- 新增 `0015_curly_ikaris.sql`：场景请求为 pending 增加处理心跳，worker 每次模型尝试前续租；超过 4 分钟后 UI 可回收陈旧占位。数据库触发器只允许仍有有效 pending 请求的 `scene:*` 命令进入事实提交，因此回收与旧 worker 恢复竞争时，至多一方能完成；回归验证 GET 无副作用、过期请求回收、活跃请求不被回收、模型调用前心跳已续期、已过期 worker 不能晚到提交。迁移回归确认现存旧请求行保留且以 heartbeat 0 降级成可回收状态。此迁移由全新隔离 SQLite 测试库应用验证；本轮未对项目默认或远端 D1 执行迁移，也未在实际 Workers 进程被强制终止的条件下做现场演练。
- 新增 `0016_active_fork_limit_guard.sql`：数据库层限制每个世界最多 3 条 active 时间线，并覆盖新建与 archived→active 重新激活，封住并发 Fork 都通过应用层计数的竞态。数据库拒绝并发超限或源快照失效时，Fork 路由将其转换为可重试的 409；故障注入验证响应冲突且不留下子时间线。隔离 SQLite 回归通过；未在 Cloudflare D1 多实例环境做并发压力，也未对项目默认或远端 D1 执行迁移。
- 新增 `0017_universe_revision_step_guard.sql`：revision 只能逐个版本前进，并要求对应时间线/expected/result version 的命令已插入；直连 SQLite 测试拒绝倒退、跳级及无命令前进，账本保持原样。审计故障注入仍能构造带命令的错误历史，报告同刻冲突的 timeline/command/version。全新测试夹具会按迁移顺序应用触发器；未对项目默认 D1 或远端执行迁移。
- 新增 `0018_smart_queen_noir.sql`：tick 运行通过 D1 条件 UPSERT 原子申请共享租约、续租与 owner-scoped 释放；每条引擎版本化命令携带 fence token，数据库 trigger 在 token 不再持有时拒绝迟到写入。SQLite 回归覆盖并发争抢单 owner、过期接管、旧 owner 无法续租/释放新租约及旧 token 写入拒绝；真实 tick API 在外部 owner 持锁时返回 409，时钟/事实不变。隔离目录 `/tmp/possibility-s01-d1.8uSr5o` 中通过 Wrangler 本地 D1 完整应用 19 条迁移，第二次执行显示无待迁移；未对项目默认本地或远端数据库执行迁移。仍没有真实 Cloudflare D1 多实例压力证据。
- `/api/engine/tick` 在 provider 请求挂起期间收到第二个并发 HTTP 请求，返回 409；首个请求恢复后 200 完成，只推进一次时钟、日程和居民决策，产生连续事实版本且 `auditUniverse` 通过。这证明同一进程的 API single-flight；不同 Worker 实例共享 D1 时的竞争仍未验证。
- 本地 API 与演示世界运行在隔离 D1；浏览器确认世界优先首页、地点/居民分布、“世界现在”面板及旧历史证据提示可见。跨 P2/P3/P4 合成账号 UI 证据现覆盖手动创建、构造、带条件分叉、比较、回到主宇宙、明确传话、真实 tick、普通聊天完成与断流恢复。
- 新增的 `legacy-migration.test.ts` 在旧 revision 数据已存在时再应用 `0017`，核对既有 head 未被改写，且合法的下一版本提交可通过、倒退/跳级仍被数据库拒绝；本检查使用隔离 SQLite 测试库，不触及默认或远端 D1。
- 世界快照 DTO 新增 `evidenceStatus`，公共只读页面不必访问私有状态端点也能区分 legacy 与 structured；状态面板优先使用状态接口结果，否则读取快照标志，并兼容较旧响应按 pinned model version 回退判断。匿名演示快照回归覆盖旧时间线无结构化证据，以及 `ensureUniverseRevision` 建立基线后的 structured 状态，且写接口仍只读。
- 普通人物创建与世界创建流程现在都会在初始化完成时建立不可变模型版本和时间线版本 0 基线。`product-journey.test.ts` 通过真实 API 路由完成 CREATE→RUN→OBSERVE→ENTER→ACT→FORK→COMPARE→RETURN；比较断言核实两线同一环境事实的不同值，返回主线后用固定模型响应继续与居民交谈，并确认交谈事实写入选定主线。该自动化覆盖不替代登录态浏览器走查。
- AC05/AC03 审计新增事实隐私与可信度链核验：每类命令产生的事实 visibility 必须符合规则（如 inform/记忆/日程 private，环境/在场对话 world）；转告信息的 certainty 必须和所引用的前序/继承知识事实一致，传闻不能被提升成已证实事实。故障注入绕过历史不可变触发器，将知识事实暴露为 world 或把转述 rumor 改成 fact，审计分别定位 `fact_visibility_projection` / `knowledge_certainty_projection` 与源命令及版本。规范提交路径回归无误报。
- 人物快捷创建、正式世界创建与其基线版本现位于同一 D1 原子批次，防止固定模型失败后残留无版本宇宙。`product-journey.test.ts` 通过触发器故障注入分别拒绝人物/默认世界和新世界的基线插入，核对所有关联行都回滚；新建的用户人物及其默认世界不会残留孤立记录。
- AC07 场景合同新增居民对访客邀约的接受/婉拒：只有访客明确邀请、居民输出有效接受、且地点属于当前世界时才创建 accepted commitment；该承诺、交谈 turns/fact/event 与请求完成状态原子提交。路由固定模型回归分别验证接受、婉拒无承诺、无明确邀请时过滤误报接受；构造者伪造接受被拒绝；事件插入故障回归证明不会留下孤立承诺。解析测试另覆盖有效/无效接受结构及邀请提示词识别。
- AC03 一致性审计新增命令—事实语义核对：移动目标、环境地点/条件/值、知识接收者/主题/内容/来源、时间推进、构造者干预、在场对话及部分系统状态事实，均检查不可变命令与不可变事实是否一致。故障注入绕过历史不可变触发器篡改环境值或知识正文后，审计按来源命令和版本报告 `fact_command_projection`；未篡改的环境→传话链审计通过。
- AC03 继续补齐命令—事实核对：NPC 对话开始/发言、邀请提议及履约状态转换、记忆摘要事实都要和原命令字段相符；故障注入改变对话发言、对话地点、邀请内容、状态目标或摘要文本时，审计按命令 ID/版本指出 `fact_command_projection`。这仍是只读核对，不构成完整的全域重放与投影重建证明。
- AC03 增加不可变起始投影证据：新建主线把各居民的初始 `person_states` 投影一并冻结在模型版本中，创建仍与宇宙/状态/版本原子提交；审计用该基线 + 本线事实对照当前状态。子线从 Fork 的不可变状态快照起算（应用实际清除分叉时未完对话的运行锁）；旧模型版本没有基线字段时不补造初始历史。固定旅程确认新世界/分叉初始审计通过，并故障注入改变初始居民心情后在 version 0 报告 `state_projection`。这是起始状态核对，不代表完成所有领域的可重放重建。
- AC05 新增普通居民决策/对话的知识边界旅程：两位居民共享同一地点，只有居民 A 获得一条私密信息；固定模型走过常规节拍提示构造及两轮 NPC 对话执行器。断言 A 的提示包含该信息，B 的节拍与对话提示均不包含私密文本；对话轮次通过命令边界写入。该证据覆盖应用实际提示构造和路由，不证明真实模型绝不会猜测或从用户输入之外泄漏。
- AC11 比较服务统一读取快照：时间线、居民状态、事件、修订版本和本地结构化事实由一次 D1 batch 获取；各 Fork 的祖先事实从随时间线固定的不可变 checkpoint 合并。对照 UI 现在展示共同过去事件样例、每侧 Fork 的改变变量/不变条件/Checkpoint 时间和版本，以及状态/事实/事件的证据时间与来源标识。`compare.test.ts` 覆盖同刻证据、不同时刻、缺失状态、旧 Fork 降级、分叉环境事实差异与 Fork 场景条件读取；新增回归确认只有分叉时间而没有不可变 checkpoint 的旧 Fork 必须标为历史不完整，但仍可读取自身分支事件。API/Web 构建通过；登录态 UI 往返旅程仍缺。
- 自动化小世界旅程覆盖进入→Fork→改变条件→在子线通过在场传话→对照；传话只在子线生成 rumor 知识，源线版本和知识保持不变，比较能显示分支差异。命令提交覆盖重复请求、同 ID 不同内容、竞争版本、中途失败回滚、状态投影审计。固定模型响应的在场对话覆盖重复请求不重复调用；故障注入证明发言、记忆、留言、交谈事实、事件及请求完成状态同批提交/回滚。新增 SSE reader 取消后查询请求状态、再以同 ID 重试的路由回归，确认请求标记 failed、模型不重跑、不留下发言，而场景开启命令与场景容器完整且可审计。登录态浏览器还验证返回主宇宙后可通过明确传话影响在场居民的知识，并准确显示传闻边界；此操作未调用模型，后续居民决策是否使用该信息仍需真实推进验证。
- Fork 边界新增多级记忆/事件回归：根线在子线 Fork 后新增的记忆和事件不会进入孙线；子线在孙线 Fork 前新增的内容会继承到孙线 checkpoint，并通过当前记忆可见性读取到。
- P3 权限回归现明确验证其他用户不能 Fork 私有世界、拥有者不能用同世界接口 Fork 另一个世界的时间线、其他用户也不能读取比较结果；三类拒绝后时间线行数不变。
- 投影审计新增约定履行/失约的确定性心情回放，并核对约定状态迁移所写关系记忆的归属、类型、内容、时间、重要度及与派生事件一致；故障注入篡改心情或关系记忆时可定位到源命令/版本。
- 居民节拍/状态命令产生的普通记忆也纳入投影核验：按不可变 action 检查 person/timeline/type/content/importance/simTime/createdAt，并尊重后续有事实支撑的 summary 归档标记；篡改记忆内容会定位到对应系统命令。
- 在场交谈事实现冻结每句公开发言文本，审计逐句核对 `dialogueTurns` 投影；居民状态命令派生的每条故事事件也按不可变 action 精确核对 ID、地点线、虚拟时间、标题、描述、类型和行动者。故障注入证明改写对话或故事文本后不再被审计器漏过。
- 在场交谈的私有副作用现在由同一不可变命令记录其预期记忆/留言 ID、归属、内容及时间；审计核对 thought/relationship 记忆和 persona 留言实际行。固定模型产品旅程分别篡改记忆与留言并验证可按源命令/版本定位，同时保持留言已读状态等后续生命周期字段可变。
- 结构化账本审计现在会报告缺失/非法命令载荷、没有审计规则的 action、未由本时间线命令支撑的 fact、fact 跨线/跨版本来源，以及单条命令产生多条主 fact；同时验证 revision 指向的固定世界模型版本仍存在。故障注入绕过 SQLite 守卫后，跨线 fact 和未支持 action 分别以明确违规报告；完整 190 项回归未误报旧数据。
- Fork 组合矩阵增加“根线接受约定→Fork 子线→孙线→根线履约”旅程：根线的后续 fulfilled fact 不会覆盖子线/孙线 checkpoint 中的 accepted 状态；对照能显示根/子线差异，两个分支的当前约定行仍为 accepted。
- 时间投影审计现从 universe revision 当前时间反向重放显式 `clock_advance.from/to`，并核对跨时钟边界之前的 facts 仍位于正确世界时刻。绕过不可变触发器把旧移动 fact 挪到推进后的时刻，审计报告 `fact_time_projection` 并指向原命令/version；对无法从 action 确定前态的显式 resident `advanceTo` 边界停止向更早历史推断。
- 提交规则现在还验证这些私有副作用的场景归属：记忆只能属于本场交谈中的居民；留言只能由在场居民留给当前访客，且位置/虚拟时间必须与对话一致。伪造外部人物记忆或转投第三方的留言均在提交前拒绝，且不产生命令/事实。
- 同一命令现在也冻结在场交谈本身的地点、参与者、世界时间和轮次上限；审计核对 `dialogues` 投影。产品旅程篡改对话地点后能按源命令/版本报告 `conversation_projection`。
- 来访场景不再在模型调用前直接写 `dialogues`：visitor `scene_open` 命令与 conversation fact 原子创建场景投影；SSE 取消会保留可审计的场景起点而不留下发言，后续交谈命令可重放同一对话投影。取消与重试的 API 回归确认审计无误报；无结构化起点的旧时间线继续不推断前史。
- 在场传话新增路由级睡眠/忙碌边界回归：睡眠或已有对话占用时返回 409，命令、事实、版本均不创建；居民清醒空闲后同一路径成功写入一次。
- 本轮只读浏览器验收确认 `http://localhost:5173/` 能渲染匿名演示首页、世界运行状态/虚拟时间、地点与居民列表、legacy 历史证据标签；展开后可读事件证据面板。此检查没有登录、创建世界或触发写入，浏览器端控制台日志未能从当前 UI 工具读取。
- 对具有不可变初始基线或 Fork checkpoint 的时间线，审计现在核对完整事件集合；直接插入没有命令来源的额外事件会报告 `unproven_event_projection`。缺少不可变起点的旧时间线继续按 legacy 降级，不推断其前史。
- 一致性审计扩展为按版本顺序对照结构化事实触及的人物状态字段（地点、活动、心情、目标、节拍水位、状态时间）及约定投影字段（状态、标题、类型、地点、截止时间、当事人、来源交谈、更新时间）；故障注入分别破坏活动字段、约定状态及约定详情，诊断包含 source command/version。审计仍不宣称能从历史重建所有投影或覆盖没有版本化证据的旧前史。
- 引擎经过的虚拟时间现在由 `clock_advance` 系统命令产生 `clock` 事实；时间线 `simNow`、real-time anchor、命令、修订版本和事实在同一个 D1 batch 原子提交，不生成误导用户的叙事事件。测试覆盖首拍只设 real-time 基准、倍速推进、投影审计与 fact 写入失败整批回滚。新增 `runTick` 固定时钟集成旅程覆盖跨日程边界转换及其跨拍幂等。
- 首次节拍的 `lastBeatSimTime` 初始化已从 tick/beat 两条直写路径迁入私有 `simulation_checkpoint` 命令/事实；同一 source key 重发只回放原命令，不生成公开事件。悬空对话占用仅在对应对话已结束/缺失时以 `dialogue_recovery` 私有事实清除；活动对话拒绝恢复，恢复命令/版本/投影在事实失败时整体回滚。`0012` 在数据库层为此增加插入前条件守卫，防止校验后占用状态变化导致“命令/事实写入但修订未前进”；回归以状态竞争故障注入验证整批回滚。状态审计能发现被重新置回的旧占用。
- 投影审计现在从不可变对话事实重建居民的 `currentDialogueId`：NPC 对话开始时验证锁定，最后一轮结束时验证解锁和 `lastBeatSimTime`。固定回归覆盖开始、未结束中间轮次、结束后的投影一致性。
- 一致性审计现在同时核对事实派生的 NPC 对话行与逐句发言行；删除已记录发言的回归会报告对应命令/版本的 `conversation_projection`。在场者若仍被进行中的另一场对话占用，scene API 明确拒绝并且不占请求 ID、不写事实。
- 知识引用规则现在保留传闻链可信度：仅可引用环境、位置、居民状态等世界可见结构化事实建立已证实消息；转述知识事实时继承其 `fact`/`rumor` 状态。居民提示上下文按接收者过滤，公共世界快照隐藏私有消息。固定 API/context 测试覆盖事实、传闻、转述与跨人物隔离；尚不能证明真实模型在任意自然对话中始终不越界。
- 新增不调用模型的 `runTick` 连续集成回归：虚拟时间跨过固定日程边界后，居民只记录一次移动/活动变化；下一拍保持同一日程时不再追加冗余事实，审计仍通过。对应修正让日程切换同时检查当前投影是否已到目标状态。
- 记忆压缩已从 executor 的直接插入/标记迁到私有 `memory_summary` 命令与事实；摘要和来源 `summarized` 标记在同一批次写入，按来源记忆集合确定幂等 ID。投影审计验证来源标记与摘要正文，测试覆盖重放、投影损坏诊断和事实写入失败整体回滚。
- 同一 real-time anchor 并发推进时的回归确认只生成一条时钟命令/事实，两个调用都收敛到同一 simNow；它使用固定隔离 SQLite 事务测试，不替代 Cloudflare D1 多实例压力测试。
- `0011_universe_revision_sim_time.sql` 为每个宇宙修订保存权威虚拟时间，并把已有修订回填到迁移时的 `timelines.simNow`，避免把旧版本未记账的自然时间流逝误判为历史损坏；兼容迁移测试覆盖已存在修订，隔离 D1 应用及空跑重检均通过。
- 本轮新增 agent 工具集成证据：simulate 的 act 在同一事务批次中推进时间线、人物状态、事实与事件；update_state 和 remember 同样产生命令/事实，居民私有记忆对应的事实标为 private。现在节拍、注入、日程切换、NPC 交谈/发言/承诺建议及 agent 行动都通过版本化提交边界。
- 新增一条跨路由世界旅程：来访者在场传话后，目标居民上下文仅获得对应 `rumor` 及来源；来访者随后通过版本化移动离场，虚拟时间推进并真实调用 `runTick` 跨过日程决策边界，固定模型请求收到原传话内容并返回警告邻居的目标，结果作为后续版本的 `resident_state` 事实提交，整线 `auditUniverse` 通过。测试可从命令/事实版本追溯“传话→居民可见知识→访客离场→引擎决策→持久状态变化”；这证明完整的确定性引擎通路，不证明真实模型必然作出该反应，也未覆盖浏览器刷新后重新进入，AC07 仍部分满足。
- 新增多级知识分支旅程：主线先获知一条消息后 Fork；之后主线与子线分别获得不同消息，子线再 Fork 出孙线。断言孙线继承根线 Fork 前与子线 Fork 前的知识，不含主线 Fork 后新增知识；根线不含子线新增知识。证明到目前为止的 `world_facts` 多级祖先 cutoff 行为，但还不覆盖全部 memory/dialogue/event 跨世界组合。
- 新增确定性七拍 `runTick` 居民旅程：每拍推进 90 个虚拟分钟，四次跨日程地点边界，固定模型节拍均成功；断言四条位置事实对应四个不同虚拟时刻、最终投影正确且整条宇宙审计通过。版本化动作现在拒绝任何人物在同一虚拟时刻被写入两个地点；人物需等待时钟推进再改变位置，拒绝不留下命令/事实。另有故障注入验证审计可识别绕过规则写入的同刻冲突事实。
- 派生事件审计从“事件 ID 存在”加强到核对与来源事实相同的时间线、虚拟时刻、事件类别、必要的对话来源及非空描述；故障注入分别把事件时间/类别改错或移至另一条线，审计能定位原命令/版本。缺事件仍单独报 `event_missing`。没有修补或重写旧叙述，缺少版本化账本的 legacy 时间线仍不由该审计推断。
- 新增共享人物跨世界上下文隔离回归：同一居民加入两个世界后，分别验证其地点/目标、接收者知识和 timeline-scoped 关系记忆都只出现在对应世界上下文；共享人物模型在既有 Fork 后被修改的固定设定测试仍验证子宇宙保留分叉时模型。该证据覆盖了指定的共享人物样例，不等于所有旧记忆、对话和事件组合矩阵。
- T09/T12 新增自然语言在场行动提议 API：只允许移动到本世界已知且不同于当前位置的地点，或向同地点清醒空闲居民转达用户原话中的逐字片段；其他动作、未知地点/接收者和模型编造内容转为澄清/拒绝。候选提议还经 T06 `validateWorldAction` 校验，响应携带 `requestId` 和状态版本，且解析接口不写命令/事实。Web 场景面板提供独立“尝试一个行动”区，提议保存在当前浏览器会话；确认前版本变化会要求重新生成。确认用同一 requestId 作为命令 ID，响应丢失后先查询命令结果；路由回归证明重复确认回放同一命令且只有一个事实。解析调用接收 HTTP abort signal；关闭场景面板会取消 SSE 请求，服务端中止正在等待的居民模型调用并把未提交请求标为失败。固定路由测试取消在途 SSE reader，验证 provider 收到 abort、请求转失败且不留下发言或模型副作用，已提交的 `scene_open` 命令/场景容器仍有完整证据；另验证连续模型失败时不提交 visitor-only 对话或私有副作用。预算触顶时提议解析不触发 provider，也不新增 LLM call log/世界事实。`world-actions/resolve.test.ts` 与 `scene/intent.test.ts` 覆盖白名单映射、澄清/拒绝、越界模型输出、未进入拒绝、零世界写入、预算拒绝、取消及命令恢复。浏览器确认、刷新、pending 请求 ID 复用及过期请求失败回收均已手工验证；仍不支持环境/时间/人物状态改变等构造者行动。
- 本轮扩展 P0 归属矩阵：另一账号对私聊读取/发送、记忆修改/删除、章节读取、时间线读取/归档，以及私人世界命令均被拒，且检查拒绝没有改写消息或记忆；公开演示已补匿名与登录两种身份的只读检查。
- Fork 读取现在在同一个 D1 批次内获取源线状态、事件、记忆、日程、承诺、结构化事实与版本；即使源线随后推进，插入触发器也会因源版本/时间不匹配拒绝旧快照。回归覆盖同一快照继承两个不同版本事实、子线再 Fork，以及捕获快照后源版本改变时拒绝创建。
- 一致性诊断现能识别居民状态事实引起的位置变化，并解析构造者干预/承诺状态变更使用的专属事件 ID；测试证明能定位被破坏的投影，并在后续新事实修正投影后恢复通过。
- 时间线内的居民记忆校正/遗忘现在属于构造者版本化行动：写入 private `memory_maintenance` 命令/事实、递增宇宙版本，并生成不含记忆正文的通用干预事件；审计核对事实与命令一致并重放最终记忆值/删除。已摘要归档的来源记忆和摘要本身不可单独编辑/遗忘，避免摘要继续携带已删除内容；`timelineId=null` 的 legacy 记忆保留旧兼容直写行为。`memories/routes.test.ts` 覆盖校正→遗忘→删除重放、版本/事实/审计，以及 legacy patch。
- 有可验证起点的时间线现在会报告起点之后新增、却没有任何命令/摘要/Fork 继承来源的 timeline-scoped memory row；旧的 `timelineId=null` 行继续作为 legacy 读取，不被误判为新事实。隔离测试验证合法 legacy row 不报错、无来源的结构化记忆定位到 `unproven_memory_projection`。这缩小了审计盲区，但不等同于所有领域的完整投影重建。
- 有完整结构化起点的时间线会将 dialogue / turn 行与 conversation 命令双向核对：既检查已提交命令的缺失/偏移，也报告没有命令来源的额外对话和发言。NPC 逐句 thought 与可选 relationship 记忆也纳入其 dialogue-turn action 的投影重放，避免孤儿检测误报合法副作用。固定审计回归直接插入无来源 dialogue+turn 后分别报告 `unproven_conversation_projection` 与 `unproven_dialogue_turn_projection`；旧的无结构化基线时间线继续不猜测历史。
- 完整结构化起点之后新增的根线承诺与在场口信也纳入孤儿投影检测：根线额外 commitment 无 checkpoint/事实时会报 `unproven_commitment_projection`；时间线口信无 conversation action 时会报 `unproven_persona_message_projection`。旧世界缺少结构化初始基线时不据此倒推历史。
- 居民日程生成不再直接写 `schedules`：引擎以 system `schedule_set` 命令和 private `schedule` fact 原子提交日程行、递增宇宙版本，不向公共事件流暴露个人作息。规则验证同一虚拟日、人物归属、地点、时间格式及重叠；提交审计对照命令和投影，检测结构化起点后的无来源日程，并按 Fork checkpoint 核验分线复制。`engine/steps/schedule.test.ts` 验证执行器实际走新入口；其他回归覆盖命令重放、owner 伪造被拒、无公开事件、孤儿日程检测及 Fork 副本篡改。
- `0009` 在数据库层禁止更新/删除 `world_commands`、`world_facts` 和 `world_model_versions`；回归直接尝试篡改/删除三类行，全部由 SQLite 拒绝且原数据保留。
- 场景请求新增归属校验的只读状态端点（missing/pending/completed/failed）；前端用 `sessionStorage` 保存请求 ID，刷新后查询状态，完成则恢复历史，失败才分配新请求 ID，处理中继续复用原 ID。API 回归覆盖已完成、失败、未提交、跨账号拒绝、相同请求 ID 并发竞争，以及相同 ID 不同输入拒绝；并发场景只触发一次模型调用及一次事实提交。本地登录态浏览器已实走刷新、pending 状态、请求 ID 复用和过期请求回收；旧 worker fencing 由 API 用例验证。

## 验收状态

| 验收 | 当前状态 | 证据与剩余差距 |
|---|---|---|
| AC01 | 通过 | 原始 79 项基线测试仍在；最新 API 224 项、Web 9 项测试共 233 项，API/Web 构建通过。新增测试连续独立生成小世界夹具，核对世界、时间线、地点和 ID 稳定；已记录基线及缺口。 |
| AC02 | 通过 | 回归覆盖旧世界、事件、对话、记忆、承诺及不完整 Fork 的读取，并确认不把旧叙述推断为新事实；匿名公共快照显式传递 legacy/structured 证据状态，UI 不因只读而误报全部是旧世界；新增迁移在代表性旧数据集上逐表比较迁移前后行内容，且全新隔离 D1 应用后重跑无待迁移项。 |
| AC03 | 部分 | 移动、环境、知识、承诺、居民节拍/日程切换/日程生成/注入、NPC 交谈、来访场景开启、agent 工具、虚拟时钟、节拍水位线、过期对话锁恢复、记忆压缩及构造者记忆维护已有版本化命令/事实与原子投影。固定响应模型的 16 拍旅程跨越至少 24 小时，每拍执行审计并覆盖 Root→Child→Grandchild；七项日程、四次跨日程变化另有 `runTick` 覆盖。`0017` revision guard 与 `0018` stale-command fence 已在 Cloudflare D1 上验证；两个远端 Worker 的 Fork 容量竞争通过。仍缺全部投影域从独立 reducer 完整重建的等价证明及更丰富居民连续生活矩阵。 |
| AC04 | 部分 | 测试覆盖命令重放、冲突、并发 CAS、场景同 ID 并发仅一次提交、过期 pending 回收与 late-worker DB guard、命令/事实/设定版本不可变、居民事实、时钟事实、节拍检查点、过期对话恢复及记忆压缩的原子回滚；人物/世界及初始模型版本在同一创建 batch，故障注入确认初始化失败全量回滚。新增路由级 SSE reader 取消→请求状态查询→同 ID 重试回归，且登录态浏览器覆盖 pending 请求复用及过期回收；全局 AC04 的完整矩阵仍未完成。 |
| AC05 | 部分 | 宇宙内知识按接收者过滤，居民上下文明确区分已证实记录/传闻/来源；转述传闻不能因引用原事实 ID 升级为已证实，私人知识不进入公共快照。固定模型回归执行普通节拍提示构造和两轮 NPC 对话路由，验证只有获知者的提示包含私密消息。新增审计核验 knowledge visibility 与引用来源 certainty，故障注入可发现私密事实公开及 rumor 被提升为 fact。尚未证明真实模型输出始终遵守知识边界，也未完成人工走查及更多转述渠道矩阵。 |
| AC06 | 部分 | 在场者须先进入，再显式移动；发起交谈的来访者被另一场进行中交谈占用时拒绝，回应者通过位置、忙碌锁与睡眠日程过滤；消息传递校验共处且可交谈。路由级回归实证睡眠/忙碌拒绝均无命令、事实或版本副作用，清醒空闲后可成功传话。时间跨日程/时区及所有忙碌状态组合尚未做完整旅程验收。 |
| AC07 | 部分 | 普通在场交谈提交版本化 conversation fact，指向具体 dialogue/turn IDs；公开发言、居民私有记忆、访客留言、事实、事件及请求完成状态在同一 batch 内原子提交；不可变命令证据可核验这些私有投影。固定响应端到端测试验证居民记忆进入后续上下文、重放不重复调用模型；产品旅程也会在交谈提交后重新构造下一拍 `EngineContext`，确认关系记忆可供居民读取；新增小世界旅程验证传话成为带来源 rumor，并经完整 `runTick` 进入固定模型请求、被用于产出居民目标变化，随后作为新版本 `resident_state` 事实提交。证明确定性引擎通路而非真实模型行为保证；刷新/离开后继续推进与真实居民多拍决策未验收。本地浏览器另验证返回主宇宙后能给居民传话，版本 v3 提示明确说明“已听到”但仍属传闻、不会自动升级为世界事实，今日模型调用为 0。仍需真实居民多拍决策验收。本轮新增结构化居民邀请回应：明确的来访者邀请被居民接受且地点有效时，接受的约定与交谈同事务提交；婉拒不创建承诺，模型在无明确邀请时误报接受也不创建；事件写入失败时承诺和对话一起回滚。已有生命周期测试分别覆盖接受/履行与截止后失约事实。仍需真实模型/人工验证多轮语境中的回应判断，并覆盖访客实际履约的完整闭环。 |
| AC08 | 部分 | 场景提交状态查询及刷新恢复已有 API 测试；T09 有限自然语言提议经过 T06 规则校验、明确确认后复用版本化动作接口，越界/歧义被澄清或拒绝且解析本身不写世界。待确认提议保存在 sessionStorage；同一 requestId 作为命令 ID，刷新/丢响应后可查已提交结果，pending 超时可由客户端请求回收，重复确认只回放一份事实。提议解析取消和场景 SSE 取消均有 API 回归，取消后状态查询确认 failed，同 ID 不重跑模型；预算触顶拒绝模型解析且不记录 LLM 调用。待登录态真实浏览器验证关闭面板和丢响应完整恢复。仍未覆盖通用动作解析及跨浏览器/存储不可用时的恢复。
| AC09 | 部分 | Fork 保存事实边界/模型版本并有源版本、时间、居民状态一致性检查；`0013` 在数据库层固定已创建 Fork 的分叉条件、来源、完整快照和祖先路径；`0014` 校验快照中的当前/未来日程；`0016` 在数据库层限制每个世界最多 3 条 active 时间线（新建和重新激活均覆盖），并将并发超限/源状态冲突转换为路由 409，故障注入确认不留子时间线。Fork 子线开放承诺可从不可变快照核对到复制投影，并检测异常额外行。自动小世界 API 旅程通过在场传话验证消息仅抵达子线、源线版本/知识不变且 Compare 呈现差异。新建 Fork 必须附带明确 what-if 与改变变量，服务端固定源线当前模拟时间；浏览器已验证条件显示及返回主线。现有隔离 SQLite/本地浏览器证据之外，Cloudflare D1 上两个独立 Worker 的最后名额竞争、跨 Worker 重放/冲突及容量拒绝也通过。尚无高并发压力证据。由于旧/legacy 线可能缺完整状态证据，且旧引擎记录没有全量投影重建保证，当前仍不能宣称任意现存 Universe 都有完整 Checkpoint。 |
| AC10 | 部分 | 结构化事实按时间线隔离、模型定义被 Fork 固定；主→子→孙知识 cutoff、嵌套 Fork 的记忆/事件/对话 checkpoint cutoff、accepted commitment 状态隔离，以及共享人物跨世界上下文隔离已有覆盖。新增单条 Root→Child→Grandchild 组合矩阵，将对话、事件、记忆、承诺、知识的 Fork 前继承和祖先分叉后隔离放入同场景，并分别审计三条线。共享人物固定名字、模型和世界描述不受后续资产编辑影响。世界首页、人物抽屉及对话详情读取冻结历史。P4 浏览器另将主线快照延迟 12 秒，在延迟响应返回前切到 Fork；旧快照未覆盖 Fork 页面。仍缺共享人物旧数据矩阵。 |
| AC11 | 部分 | Compare API 在单个 D1 batch 快照中对齐时间线、人物状态、事件、修订与本地事实，并从不可变 Fork checkpoint 继承共同过去；无快照的旧 Fork 即使具有分叉时间也明确标记历史不完整，同时保留其自身事件。对照 UI 展示分叉条件、Checkpoint 与共同事件样例，差异显示证据 ID/版本/时间。浏览器检查确认条件、共同祖先、环境事实值、证据 ID/版本及对齐时间，且能返回主宇宙；仍需对所有历史/因果文案作人工场景走查。 |
| AC12 | 通过 | API 固定旅程覆盖 CREATE→RUN→OBSERVE→ENTER→ACT→FORK→COMPARE→RETURN，并在返回主线继续交谈。跨 P2/P3/P4 登录态本地浏览器记录覆盖世界/人物创建和行动、Fork/Compare 返回、实际 tick 推进、普通聊天完成/断流以及返回时间线恢复；所有旅程使用固定模型替身。 |
| AC13 | 通过 | 三种模式边界和记忆权限由组件回归覆盖。新增长线态浏览器实测：在一次性 D1 插入真实 timeline memory，构造模式校正后刷新仍显示新内容且 revision 从 0 到 1；观察模式只读显示且无改/删操作；回到构造模式遗忘该记忆，D1 记录删除且 revision 到 2。摘要不可单独编辑/删除另由 `PersonDrawer.test.tsx` 与 API 记忆维护测试覆盖。 |
| AC14 | 部分 | 新增路由有归属/时间线检查，跨用户世界/状态/交谈读取及命令写入被拒，公开演示写入口拒绝，暂停世界/归档线写入无半成品；Hono 命令 API 测试验证跨用户、同用户错世界时间线、paused、capped、archived 均拒绝且不留下 command/fact/revision；Fork/Compare 回归验证跨用户及跨世界时间线被拒且不创建子线。旧聊天发送现在先过世界/时间线/预算闸门，暂停或归档后不会残留被拒绝的用户消息，已有聊天仍可读。预算触顶与全部旧/新入口的完整权限矩阵仍缺。 |
| AC15 | 未满足 | 最新 API 224 项、Web 9 项自动测试通过；revision guard 在真实 Cloudflare D1 上拒绝越级写入。两个独立 Wrangler remote Worker 共用真实 D1，最后一个 Fork 名额竞争返回 200/409、跨 Worker 重放返回 200、载荷冲突和容量拒绝返回 409，数据库投影计数相符；两个独立远端 D1 请求竞争原子 tick lease UPSERT 时恰有一个 owner；持有外部 D1 租约时两个 Worker `/api/engine/tick` 均返回 409 且没有世界副作用。新增远端过期 owner 模拟：两个 Worker 同时请求 tick 时只有一个接管成功（200）、另一个为 409；成功者推进 revision 和 clock fact 后释放 lease。真实 Worker 进程被强制中止后的接管、取消/恢复与多级 Fork 的统一端到端矩阵仍未完成。本地双 Worker 另覆盖 tick 完成/租约释放、源写入竞争和故障回滚。P2–P4 登录态浏览器旅程证据齐全；AC15 仍是 S01 全局出口阻塞项。 |

## 阶段判断

- **P0：具体兼容与权限检查已通过，阶段闸门仍未通过。** 旧数据迁移/读取、固定夹具重复生成、私人接口跨账号拒绝和公开演示只读均有测试；全局 AC15 要求的取消/恢复/并发/多级 Fork 全矩阵尚未完成。
- **P1：关键底座已建立，出口未通过。** 世界变化主路径、虚拟时钟推进、节拍水位线、过期对话占用恢复、日程生成/切换和记忆压缩/维护已迁入版本化提交边界；日程边界跨拍重复记账已修正并由 `runTick` 回归覆盖。审计能核对居民状态、事件、承诺、对话/发言、口信、记忆及日程的主要投影，并发现结构化基线后的部分孤儿投影。仍缺全量投影重建/一致性门槛、完整居民多拍连续推进及 AC14/AC15 全矩阵。
- **P2：S01 P2 出口通过。** 意图与失败恢复回归覆盖逐字传话、缺席居民澄清、过期版本、请求 ID 冲突和无居民回应时不留 visitor-only 半成品；登录态本地浏览器完成进入、移动、明确行动确认、交谈、刷新/重开恢复、pending 请求复用、过期 SSE 请求回收及返回观察页；固定模型小世界旅程证明在场口信进入后续居民决策并形成可审计的新版本结果。隔离浏览器节拍使用的固定替身不实现 schedule 的 `items` 输出，因此该 UI 演示不作为居民日程/后续影响证据。
- **P3：S01 P3 出口通过。** 隔离本地 D1 上两个 Worker 的真实 Fork/源写入竞争、幂等、容量拒绝和故障回滚通过；Root→Child→Grandchild 记忆/承诺/知识组合矩阵及三线审计通过；消息来源进入接收者上下文并对旁支/其他居民保持隔离；登录态 Compare 浏览器走查覆盖条件证据、不同模拟时刻、legacy 不完整提示及返回选线。详细的状态码、账本结果、浏览器实际文案、命令和边界见本报告后附的“S01 P3 本轮验收”。P4 阶段出口通过，证据见下节。
- **P4：阶段出口通过。** 登录态浏览器完成真实本地 tick、普通聊天完成/断流恢复、迟到聊天回复切线隔离、延迟旧快照到达隔离及 Fork→Compare→返回；匿名 demo 首页只读控件边界通过。API/Web 全量测试及构建通过。传输层旧世界 SSE 帧未单独延迟注入，清理订阅和 timeline 归属由前端守卫单测覆盖。

上述缺口不是文案层面的免责声明，而是当前实现的范围边界。特别是，在 P1/P2 缺口关闭前，不能把“版本化命令”描述成全世界唯一权威历史，也不能把普通交谈描述成必然改变模拟结果。

## S01 P2 本轮实施与验收

详细的 AC1–AC9 命令、浏览器旅程、临时环境和限制见 [`docs/current-state-audit.md`](./current-state-audit.md) 的“P2 实施与验收证据”及 [`spec_docs/s01/checklist.md`](../spec_docs/s01/checklist.md)。本轮自动化结果：scene intent 12/12、atomic commit 35/35、scene recovery 2/2、world journey 5/5、legacy/public 10/10；T10 汇总 intent/recovery/world journey 19/19。API、Web 构建均通过。

P2 检查未增加产品模块、数据库表或迁移；新增在场失败路径保护及回归测试。登录态验收完全使用临时 D1、虚构账号和本机模型替身；未部署或访问远端数据库。浏览器通过刷新/重开验证已完成交谈及行动历史恢复，通过 pending/过期请求旅程验证状态查询、同 ID 复用及失败后的新请求提示；旧 worker fencing 和无半成品原子失败由 API 回归证明。真实模型文风或临时 schedule 替身行为不计为出口证据。S01 P2 AC1–AC9 均有通过证据，阶段出口通过。

## S01 P3 本轮验收

P3 关闭四项原报告缺口。规格和逐条结果见 [`spec_docs/s01/spec.md`](../spec_docs/s01/spec.md)、[`spec_docs/s01/checklist.md`](../spec_docs/s01/checklist.md)；环境、命令、HTTP 状态及数据库快照见 [`docs/current-state-audit.md`](./current-state-audit.md) 的“P3 实施与验收证据”。

- **AC1 多 Worker Fork：通过。** readiness 确认 Worker 共享隔离 D1；完整脚本退出码 0。一次最终轮记录不同 Fork 并发 200/409、同载荷重放 200、同 ID 不同载荷 409；源 Fork/写入竞争 200/409，成功分支的 checkpoint、revision 和 person state 完整，拒绝行动无 command/fact；活动线容量返回 409；故障注入返回预期 500 且 timeline/revision/state/command/fact 均为 0。tick lease 竞争 409/200，恰有一个 clock fact、无遗留 lease。Worker 日志中的 SQLITE_BUSY 与注入触发器失败是明确覆盖的竞争及回滚场景。
- **AC2 多级隔离：通过。** 新增组合矩阵逐条比较 Root、Child、Grandchild 的记忆、接受承诺和知识 checkpoint；祖先后写不下渗，子线 Fork 前内容仅进入后代 checkpoint，后写不反向污染。三线 `auditUniverse` 为空。
- **AC3 消息旅程：通过。** 固定旅程记录 timeline、fact ID、version 和 `rumor` certainty；正确接收者的后续决策上下文含来源证据，另一居民、Root 和旁支不可见。登录态在场 UI 显示“对方已听到这条消息（v2）；它仍是传闻，不会自动变成世界事实。”
- **AC4 Compare UI：通过。** 合成账号浏览器检查结构化分叉条件、checkpoint 来源和因果限制；临时不同模拟时刻显示时间未对齐；无完整 checkpoint 的 legacy Fork 标为历史证据不完整；关闭 Compare 后保留被选中的 Child。
- **AC5 权限和失败副作用：通过。** 27 项 Compare/Fork 回归与 6 项 world journey 回归通过；覆盖跨账号/错配时间线、只读 Compare、归档 Fork 源拒绝无写入、容量边界、Fork 错误回滚和多 Worker 竞争账本一致性。
- **AC6 报告与构建：通过。** 定向 API 测试合计 33/33；API TypeScript build、Web TypeScript + Vite production build 均成功。测试和浏览器仅用合成账号、固定模型替身和一次性本地 D1，验收结束后停止服务并清理临时库。

**P3 最终命令：** `npm run verify:s01:workers -- --readiness-only`；`npm run verify:s01:workers`；`npm --workspace api run test -- src/life/compare.test.ts src/test/world-journey.test.ts`；`npm --workspace api run build`；`npm --workspace web run build`。以上全部通过。没有访问远端/生产 D1、部署或真实模型。仍保留 legacy Fork 历史 unknown 边界；Compare 只呈现记录差异，不证明因果；消息进入接收者知识不保证其已阅读、相信或采取行动。S01 P3 AC1–AC6 均有可复核证据，阶段出口通过。

## S01 P4 本轮验收

实现变更、逐 AC 浏览器证据、Worker/D1 审计数及未验证范围见 [`docs/current-state-audit.md`](./current-state-audit.md) 的“S01 P4 实施与验收证据”。Web workspace 没有组件级 DOM runner；旧快照竞态用本地代理在浏览器中实际延迟，旧订阅及 timeline 归属由 Web 单元测试覆盖。

- **AC1/AC2：通过。** 世界 URL 从 query 选择并保持时间线，刷新后恢复；浏览器观察暂停/继续和本地 pinger 推进。最新 Worker 脚本运行记录 1 条 clock fact、revision 2、模拟时间推进且无活动 lease。
- **AC3：通过。** 普通聊天在本地浏览器取得完整固定回复并保留主线归属；SSE 断流后 UI 明确报连接错误，刷新可见用户输入仍在历史、半截回复不在历史。API 测试覆盖时间线拒绝、完整持久化及断流重试。
- **AC4/AC5：通过。** 普通聊天后世界版本仍为 v0、事件为 0；实际 Fork 带明确条件并进入 Compare，能返回主线继续进入聊天。
- **AC6：通过。** 除 URL 切换、刷新恢复和延迟分支聊天隔离外，本地代理将旧主线第二次快照响应延迟 12 秒；创建 Fork 并选中后才释放，页面仍停留在 Fork，v0 和 0 条事件未变。`WorldView` 单测覆盖 timeline 已切换、effect 已清理及响应 ID 不匹配时拒绝更新。传输层旧世界 SSE 帧未单独做延迟注入。
- **AC7：通过。** 合成 demo fixture 下，匿名页面可浏览公开状态且只显示登录入口；公开路由自动化拒绝匿名写入。
- **AC8：通过。** API 32 个测试文件/224 项通过，Web 2 个文件/9 项通过，API/Web build 与 `git diff --check` 通过；`npm run verify:s01:workers` 退出码 0。临时 D1 和本地服务均已清理，AC1–AC7 均有对应证据，S01 P4 阶段出口通过。

本轮未部署或访问远端/生产数据库，未调用真实模型。传输层旧世界 SSE 帧没有独立的延迟注入证据，其回调清理和 timeline 归属由前端守卫单测覆盖。
